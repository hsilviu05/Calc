<Window x:Class="Calc.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calc.Views"
        mc:Ignorable="d"
       Title="About Calculator" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Calculator Application" 
                FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="A Windows Calculator clone with Standard and Programmer modes"
                TextWrapping="Wrap" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Developed by: Hermeneanu Ionut Silviu" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="3" Text="Group: LF232" Margin="0,0,0,5"/>

        <TextBlock Grid.Row="4" Text="Version: 1.0.0" Margin="0,0,0,5"/>

        <Button Grid.Row="4" Content="Close" HorizontalAlignment="Right"
             Width="75" Click="CloseButton_Click" Margin="0,10,0,0"/>
    </Grid>
</Window>
